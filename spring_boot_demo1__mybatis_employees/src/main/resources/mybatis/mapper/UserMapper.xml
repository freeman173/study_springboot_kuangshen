<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.spring_boot_demo1.mapper.EmployeeMapper">

    <select id="queryEmployeeList" resultType="Employee">
        select id,lastName,email,gender,departmentid,birth from employees
    </select>

    <select id="queryEmploybyId" resultType="Employee">
        select id,lastName,email,gender,departmentid,birth from employees where id=#{id}
    </select>

<!--    插入除了id之外的元素就行，id自动生成，可能会有问题，到时在调试-->
    <insert id="addEmployee" parameterType="Employee">
        insert into employees (lastName,email,gender,departmentid,birth) values (#{lastName},#{email},#{gender},#{departmentid},#{birth})
    </insert>

    <update id="updateEmployee" parameterType="Employee">
        update employees set lastName=#{lastName},email=#{email},gender=#{gender},departmentid=#{departmentid},birth=#{birth} where id =#{id}

    </update>

    <delete id="deleteEmployee" parameterType="Integer">
    delete from employees where id=#{id}
    </delete>

</mapper>
